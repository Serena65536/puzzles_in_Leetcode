class Solution:
    def fib(self, n: int) -> int:
        f=[None]*(n+1)
        f[0]=0
        if n>0:
             f[1]=1
        if n>1:
            for i in range(2,n+1):
                f[i]=f[i-1]+f[i-2]
        return f[n]
